// This line first call the Package means which consist folder name as package.
package Basic;
// This line having a class name or may be the page name 
class Question1{
    //   Public means Access modifier: Means this method can be called from anywhere by the Java runtime, by other classes.
    // Static Means belong to class method itself doesn't need to any other class object to call this program.
    // Void means this method does not return any value.
    // Main a special name that the Java Virtual Machine (JVM) looks for as the program’s entry point.
    public static void main(String args[]){
        System.out.println("Java is better than C++");
    }
}